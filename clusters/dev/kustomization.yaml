apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Dev cluster bootstrap
# Apply this to bootstrap the dev cluster:
#   kubectl apply -k clusters/dev/
resources:
  - ../../bootstrap/root-app.yaml
